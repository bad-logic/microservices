name: ticket_test

on: 
  pull_request:
    - 'tickets/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: cd tickets && npm i && npm run test:ci
